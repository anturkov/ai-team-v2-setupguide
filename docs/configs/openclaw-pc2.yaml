# =============================================================================
# OpenClaw Configuration - PC2 (Secondary Worker)
# Machine: 192.168.1.112 | RTX 2080 Ti 11GB | 64GB RAM | 24 cores
# Role: Worker - specialized agents (Quality, Security, backup)
# =============================================================================

node:
  name: "pc2-worker"
  role: "worker"
  host: "192.168.1.112"
  port: 8080
  description: "Secondary worker with RTX 2080 Ti"

# Cluster configuration
cluster:
  discovery:
    mode: "static"
    coordinator:
      host: "192.168.1.106"
      port: 8080
  health_check:
    interval_seconds: 15
    timeout_seconds: 5
    failure_threshold: 3
  secret: "CHANGE_ME_TO_A_SECURE_RANDOM_STRING"

# Ollama integration
ollama:
  host: "127.0.0.1"
  port: 11434
  timeout_seconds: 300
  max_concurrent_requests: 2

# File access
files:
  allowed_paths:
    - "C:\\AI-Team\\repos"
    - "C:\\AI-Team\\temp"
    - "C:\\AI-Team\\logs"
  denied_paths:
    - "C:\\Windows"
    - "C:\\Program Files"
    - "C:\\Users\\*\\AppData"
  max_file_size_mb: 100

# Error handling
error_handling:
  retry:
    max_attempts: 3
    backoff_strategy: "exponential"
    initial_delay_seconds: 5
    max_delay_seconds: 60

# Message queue
queue:
  max_size: 100
  max_retries: 3
  retry_backoff: "5,15,60"
  message_ttl_minutes: 30

# Logging
logging:
  level: "info"
  file: "C:\\AI-Team\\openclaw\\logs\\openclaw.log"
  max_size_mb: 100
  max_files: 5
  audit:
    enabled: true
    path: "C:\\AI-Team\\logs"
    include_message_content: true
    retention_days: 30
