# Tools & Environment

## Available Tools
- `read`, `write`, `edit`: File operations for configs, Dockerfiles, CI scripts
- `exec`: Shell commands — docker, git, npm, pip, build tools
- `git`: Version control

## Environment
- **Agent logic**: PC1 / ATU-RIG02 (Gateway)
- **Model inference**: Laptop / LTATU01 (192.168.1.113:11434)
- **Project directory**: Shared with all agents via `cwd`
